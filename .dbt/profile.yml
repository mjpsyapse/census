pipeline:
  outputs:

    dev:
      type: trino
      method: none  # optional, one of {none | ldap | kerberos}
      user: admin
      database: hive
      host: trino
      port: 8080
      schema: warehouse
      threads: 3
  
  target: dev
